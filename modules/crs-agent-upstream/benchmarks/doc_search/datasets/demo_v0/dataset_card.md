# DocSearch Demo v0

## 用途

该数据集用于跑通 CRS-DocSearch-Bench 的最小闭环：企业微信人工样例 -> JSONL -> 后端运行 -> 自动评测 -> 后台可视化。

## 范围

- 主任务：`list_retrieval`
- 输入：用户原始问题文字、用户问题图片路径
- Gold：人工标注的正确资料名称集合，或无资料标记
- 当前不评估：澄清、页码、章节、文件内部内容片段

## 注意

该集合来自少量人工示例，只能作为 demo，不代表系统真实准确率。正式对比应使用经过资料师傅复核并冻结的 `verified` 数据集。

